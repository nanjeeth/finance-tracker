services:
  backend:
    build: ./backend
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/finance.db
    expose:
      - "8000"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  db-data:
